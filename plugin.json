{
  "name": "Outfunnel PostHog Export Plugin",
  "url": "https://github.com/outfunnel/outfunnel-posthog-export-plugin",
  "description": "Export PostHog events to Outfunnel.",
  "main": "dist/index.js",
  "posthogVersion": ">= 1.25.0",
  "config": [
    {
      "markdown": "Sign up for a [Outfunnel.com](https://www.outfunnel.com) account, and follow the instructions in app"
    },
    {
      "key": "outfunnelApiKey",
      "name": "Outfunnel API Key",
      "type": "string",
      "default": "",
      "hint": "The API key for your Outfunnel account. You can find it in the Outfunnel app.",
      "required": true,
      "secret": true
    },
    {
      "key": "uploadMinutes",
      "name": "Upload at most every X minutes",
      "type": "string",
      "default": "1",
      "hint": "If there are events to upload and this many minutes has passed since the last upload, send the events to Outfunnel. The value must be between 1 and 60 minutes.",
      "required": true
    },
    {
      "key": "uploadMegabytes",
      "name": "Maximum upload size in megabytes",
      "type": "string",
      "default": "1",
      "hint": "Always keep the uploaded files below this size limit, uploading more frequently than the time limit if needed. Events are kept in memory until then, so make sure your server has enough of it. The value must be between 1 and 100 MB.",
      "required": true
    },
    {
      "key": "eventsToIgnore",
      "name": "Events to ignore",
      "type": "string",
      "default": "$feature_flag_called",
      "hint": "Comma separated list of events to ignore"
    },
    {
      "key": "uploadFormat",
      "name": "Format of uploaded files",
      "type": "choice",
      "choices": ["jsonl"],
      "default": "jsonl",
      "hint": "jsonl: text files with one event per line as JSON"
    }
    ]
}
